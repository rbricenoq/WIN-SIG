

abrir php.ini

buscan mail y dejan esto asi:

[mail function]
; For Win32 only.
; http://php.net/smtp
;SMTP=smtp.gmail.com
; http://php.net/smtp-port
;smtp_port=587

; For Win32 only.
; http://php.net/sendmail-from
;sendmail_from = wintig2016ii@gmail.com

; For Unix only.  You may supply arguments as well (default: "sendmail -t -i").
; http://php.net/sendmail-path
sendmail_path = "\"C:\xampp\sendmail\sendmail.exe\" -t"

luego abren sendmail.ini
comentarean todo y añaden esto:

;new config:

smtp_server=smtp.gmail.com
smtp_port=587
smtp_ssl=auto
error_logfile=error.log 
auth_username=wintig2016ii@gmail.com
auth_password=wintig2016

modificar tabla usuario

ALTER TABLE winsig.usuario
    ADD COLUMN activate varchar(32) NOT NULL;

ALTER TABLE winsig.usuario
    ADD COLUMN estado integer NOT NULL;
